# XLpkg

Local scaffold for the XLuau package registry index.

The real default registry lives at:

`https://raw.githubusercontent.com/XLuau/XLpkg/main/index.json`

This local folder is here so `xluau publish` and registry-oriented tests have a repo-native place
to write an `index.json` without depending on external infrastructure.
