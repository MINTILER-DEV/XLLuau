const PI = 3.14

local { x, y: posY, role = "user", ...rest } = point
local [head, _, third, ...tail] = values

function update({ x, y }: Point, [first, ...others]: ArrayLike)
    return x, y, first, others
end

for { name, score } in players do
    print(name, score)
end
